name: CI Build (Reusable)
'on':
  workflow_call:
    inputs:
      services:
        description: JSON list of services to build
        required: true
        type: string
      registries:
        description: JSON list of registry URLs
        required: true
        type: string
      build_args:
        description: Additional Docker build arguments
        required: false
        type: string
        default: ''
permissions:
  contents: read
  pull-requests: read
  packages: write
  id-token: write
jobs:
  detect-changes:
    runs-on: ubuntu-latest
    outputs:
      matrix: ${{ steps.changes.outputs.changes }}
      has_changes: ${{ steps.changes.outputs.changes != '[]' }}
    steps:
    - name: Checkout
      uses: actions/checkout@v4
      with:
        fetch-depth: 0
    - name: Detect changes
      id: changes
      uses: dorny/paths-filter@v3
      with:
        filters: "src:\n  - '**'"
        list-files: json
  lint:
    runs-on: ubuntu-latest
    needs:
    - detect-changes
    if: ${{ needs.detect-changes.outputs.has_changes == 'true' }}
    steps:
    - name: Checkout
      uses: actions/checkout@v4
    - name: Lint
      run: echo 'Linting step - configure for your language'
  test:
    runs-on: ubuntu-latest
    needs:
    - lint
    steps:
    - name: Checkout
      uses: actions/checkout@v4
    - name: Run tests
      run: echo 'Test step - configure for your language'
  build:
    runs-on: ubuntu-latest
    needs:
    - test
    outputs:
      image_tag: ${{ steps.meta.outputs.tags }}
      image_digest: ${{ steps.build.outputs.digest }}
    steps:
    - name: Checkout
      uses: actions/checkout@v4
    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3
      with:
        driver-opts: image=moby/buildkit:latest
    - name: Docker metadata
      id: meta
      uses: docker/metadata-action@v5
      with:
        images: ${{ (inputs.registries == '' || inputs.registries == '[]') && 'local-image' || inputs.registries }}
        tags: type=sha,prefix=sha-,format=short
    - name: Build image
      id: build
      uses: docker/build-push-action@v5
      with:
        context: .
        push: false
        load: true
        tags: ${{ steps.meta.outputs.tags }}
        labels: ${{ steps.meta.outputs.labels }}
        cache-from: type=gha
        cache-to: type=gha,mode=max
  scan:
    runs-on: ubuntu-latest
    needs:
    - build
    steps:
    - name: Run Trivy vulnerability scanner
      uses: aquasecurity/trivy-action@master
      with:
        image-ref: ${{ needs.build.outputs.image_tag }}
        format: sarif
        output: trivy-results.sarif
        severity: CRITICAL,HIGH
        exit-code: '1'
    - name: Upload Trivy scan results
      uses: github/codeql-action/upload-sarif@v3
      if: always()
      with:
        sarif_file: trivy-results.sarif
  push:
    runs-on: ubuntu-latest
    needs:
    - scan
    if: ${{ inputs.registries != '' && inputs.registries != '[]' }}
    steps:
    - name: Checkout
      uses: actions/checkout@v4
    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3
    - name: Docker metadata
      id: meta
      uses: docker/metadata-action@v5
      with:
        images: ${{ inputs.registries }}
        tags: type=sha,prefix=sha-,format=short
    - name: Build and push
      uses: docker/build-push-action@v5
      with:
        context: .
        push: true
        tags: ${{ steps.meta.outputs.tags }}
        labels: ${{ steps.meta.outputs.labels }}
        cache-from: type=gha
        cache-to: type=gha,mode=max
